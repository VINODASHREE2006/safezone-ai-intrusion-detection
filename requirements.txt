# Core CV + numerics
opencv-python-headless==4.8.1.78
numpy==1.26.4

# Config + data
PyYAML==6.0.1
pandas==2.2.2

# Dashboard
streamlit==1.32.0

# Optional: only needed if you want browser-friendly H.264 output.
# ffmpeg is a SYSTEM package, not a pip one:
#   Ubuntu/Debian : sudo apt install ffmpeg
#   macOS         : brew install ffmpeg
#   Windows       : https://www.gyan.dev/ffmpeg/builds/
# Without it the app still works - it streams annotated frames live and
# writes an mp4v file you can download.
